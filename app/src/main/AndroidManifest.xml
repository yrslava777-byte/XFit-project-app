<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XFitApplication">

        <!-- Главный экран запуска (Onboarding) -->
        <activity
            android:name=".presentation.screens.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Главный экран приложения (Dashboard) -->
        <activity
            android:name=".presentation.screens.DashboardActivity"
            android:exported="false" />

        <!-- Экран поиска продуктов -->
        <activity
            android:name=".presentation.screens.SearchActivity"
            android:exported="false" />

        <!-- MainActivity (заглушка, пока не используется) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>